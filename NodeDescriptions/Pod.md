# Pod

## Overview

A running Pod. Source of privilege escalation edges to Nodes (`podPrivileged`, `hostPID`, etc.), `compromiseServiceAccount` to its ServiceAccount, and volume mount edges to Volumes.

## Scope

Namespaced
