# CH_Namespace

## Overview

A Kubernetes namespace, derived from collected resources. Acts as the target for namespace-scoped RBAC edges (`CH_canExec`, `CH_canPatch`, `CH_canBind`, etc.) when no `resourceNames` restriction is present.

## Scope

Cluster
