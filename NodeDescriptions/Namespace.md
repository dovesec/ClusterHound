# Namespace

## Overview

A Kubernetes namespace, derived from collected resources. Acts as the target for namespace-scoped RBAC edges (`canExec`, `canPatch`, `canBind`, etc.) when no `resourceNames` restriction is present.

## Scope

Cluster
