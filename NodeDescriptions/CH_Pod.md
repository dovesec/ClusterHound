# CH_Pod

## Overview

A running Pod. Source of privilege escalation edges to Nodes (`CH_podPrivileged`, `CH_podHostPID`, `CH_podHostNetwork`, `CH_podHostIPC`), `CH_compromiseServiceAccount` to its ServiceAccount, and volume mount edges to Volumes.

## Scope

Namespaced
