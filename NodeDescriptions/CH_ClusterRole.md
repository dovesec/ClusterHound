# CH_ClusterRole

## Overview

A cluster-scoped RBAC ClusterRole. Target of `CH_bindsRole` edges from RoleBindings and ClusterRoleBindings. Retained in the graph to preserve the structural binding chain.

## Scope

Cluster
