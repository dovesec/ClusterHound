# Volume

## Overview

A volume declared in a Pod spec. Target of `hasReadVolume` and `hasWriteVolume` edges. Resolved through PVC → PV chains where applicable and enriched with volume type and source path.

## Scope

Namespaced
