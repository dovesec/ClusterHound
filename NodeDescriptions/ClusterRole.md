# ClusterRole

## Overview

A cluster-scoped RBAC ClusterRole. Target of `bindsRole` edges from RoleBindings and ClusterRoleBindings. Retained in the graph to preserve the structural binding chain.

## Scope

Cluster
